import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.Parameterized;
import java.lang.StringBuilder;

import java.io.FileNotFoundException;
import java.util.Arrays;
import java.util.Collection;


    @RunWith(value = Parameterized.class)
    public class FileWorkerTest {
        final private FileWorker fileWorker = new FileWorker();
        private String fileName;
        private StringBuilder loadingLine;

        public FileWorkerTest (String fileName, StringBuilder loadingLine) {
            this.fileName = fileName;
            this.loadingLine = loadingLine;
        }

        @Parameterized.Parameters
        public static Collection<Object[]> data() {
            return Arrays.asList(new Object[][]{
                    {"File.txt", new StringBuilder().append("IJK")},

            });


        }

        @Test(timeout = 3000, expected = FileNotFoundException.class)
        public void readTextFromFile() throws Exception {
            assertEquals("Not equals lines", loadingLine, fileWorker.readFromFile("File.txt"));
        }

        private void assertEquals(String s, StringBuilder loadingLine, String s1) {
        }

    }
